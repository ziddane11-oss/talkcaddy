import { useEffect, useState } from 'react';

export const useErrorWebSocket = () => {
  // 빌드 에러 방지용 임시 코드
  console.log("Mock useErrorWebSocket");
  return [];
};

export default useErrorWebSocket;
